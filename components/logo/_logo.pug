//- Logo
mixin logo
	// Logo
	.logo&attributes( attributes )
		a.logo-link( href= homePage.link )
			+lazy-img( logo.default ).logo-image-default( alt= logo.name )
			+lazy-img( logo.inverse ).logo-image-inverse( alt= logo.name )
